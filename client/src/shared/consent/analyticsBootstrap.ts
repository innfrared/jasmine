/**
 * Placeholders for third-party analytics / marketing tags (GA, GTM, pixels, etc.).
 * Real script loading is wired here later; consent gating must call these only when allowed.
 */

export function enableExternalAnalytics(): void {
  // Intentionally empty — replace with GA/GTM bootstrap when ready.
}

export function enableExternalMarketing(): void {
  // Intentionally empty — replace with marketing pixels when ready.
}
